;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;;
;; versions and autosave

(setq version-control nil)
(setq auto-save-interval 400)
(setq auto-save-timeout 360)
(setq kept-new-versions 3)
(setq find-file-run-dired t)
